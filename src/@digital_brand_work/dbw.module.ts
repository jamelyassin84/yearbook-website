import {NgModule} from '@angular/core';

const components = [];

@NgModule({
  // declarations: [...components],
  // imports: [CommonModule],
  // exports: [...components],
})
export class DBWModule {}
